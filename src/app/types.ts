export type Project = {
  num: string;
  category: string;
  title: string;
  description: string;
  link: string;
  image: string;
  live: string;
};

export interface workSlider {
  containerStyles: string;
  btnStyles: string;
}
